#include <iostream>
using namespace std;

int main()
{
    //chars and strings
    char grade = 'A'; //Chars need ''
    string name = "Indra is based times 3"; //Strings need ""

    //numbers
    int age = 50; //no decimal point
    float flnum = 4.5; //allows for decimals but only up to a certain number of D.P which is less than double
    double dubnum = -428.493; //same as float but supports longer decimals
    //realistically, just use a double over a float unless specified to.

    //Boolean
    bool isFemale = true; //


    return 0;
}
